#include <stdio.h>

/**
 * main - Entry point
 * author: Cynthia
 * Return: Always 0(pass)
 */
int main(void)
{
	int base;

	for (base = 48; base < 58; base++)
		putchar(base);
	putchar('\n');
	return (0);
}
